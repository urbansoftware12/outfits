import 'package:flutter/material.dart';

void main() {
  runApp(const OutfitsApp());
}

class OutfitsApp extends StatelessWidget {
  const OutfitsApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

